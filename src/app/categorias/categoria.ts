export interface categoriaDTO {
  idCategoria: number;
  nombre: string;
  descripcion: string;
  estado: boolean;
}

export interface categoriaCreacionDTO {
  nombre: string;
  descripcion: string;
}
